version: "3.7"
services:
  guideline-interface:
    build:
      context: apps/guideline-interface/
    ports:
      - "5001:80"
  ui-backend:
    build:
      context: apps/ui-backend
    ports:
      - "5002:80"
    volumes:
      - comparator-results:/var/data
  clinical-data-interface:
    build:
      context: apps/clinical-data-interface
    ports:
      - "5003:80"
  compliance-evaluator:
    build:
      context: apps/compliance-evaluator
    ports:
      - "5004:80"
    volumes:
      - comparator-results:/var/data
  ui:
    build:
      context: apps/ui
    ports:
      - "5000:3838"

volumes:
  comparator-results:
